// Aircall — Phase 1. Auto-logs calls from the dialer. Solves the biggest blind
// spot in the old app (reps live in their dialer, not in the tracker).
export const AIRCALL_PROVIDER_ID = "aircall" as const;
