// Stripe — covers two surfaces:
//   1. Stripe Billing for our own SaaS subscription (Phase 4)
//   2. Stripe payment-processor integration for customers using Stripe (Phase 3)
export const STRIPE_PROVIDER_ID = "stripe" as const;
