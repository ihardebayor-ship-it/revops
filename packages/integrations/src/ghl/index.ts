// GoHighLevel — Phase 1 stub. Real implementation lands when we wire the
// inbound-appointment workflow. Pattern: client.ts (typed API client), oauth.ts
// (OAuth flow), signature.ts (webhook verification), types.ts (Zod), events.ts
// (event types we care about).
export const GHL_PROVIDER_ID = "gohighlevel" as const;
